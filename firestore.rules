rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users can read/write the good boy tracker
    match /good_boy_tracker/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
